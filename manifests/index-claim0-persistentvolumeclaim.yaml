apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: index-claim0
  name: index-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
